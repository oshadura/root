add_root_subdirectory(gui ALL)
add_root_subdirectory(ged ALL)
add_root_subdirectory(fitpanel ALL)
add_root_subdirectory(guibuilder ALL)
add_root_subdirectory(guihtml ALL)
add_root_subdirectory(recorder ALL)
add_root_subdirectory(sessionviewer ALL)
if(root7)
  add_root_subdirectory(webdisplay ALL)
  if(cefweb)
  add_root_subdirectory(cefdisplay ALL)
  endif()
  if(qt5web)
  add_root_subdirectory(qt5webdisplay ALL)
  endif()
  add_root_subdirectory(canvaspainter ALL)
  add_root_subdirectory(fitpanelv7 ALL)
endif()
if(qtgsi)
  add_root_subdirectory(qtgsi ALL)
endif()
if(qt)
  add_root_subdirectory(qtroot ALL)
endif()
