add_root_subdirectory(io BASE)  # special CMakeLists.txt
add_root_subdirectory(rootpcm BASE)
add_root_subdirectory(sql ALL)
add_root_subdirectory(xml BASE)
if(xml)
  add_root_subdirectory(xmlparser ALL)
endif()
if(castor)
  add_root_subdirectory(castor ALL)
endif()
if(rfio)
  add_root_subdirectory(rfio ALL)
endif()
if(gfal)
  add_root_subdirectory(gfal ALL)
endif()
if(dcache)
  add_root_subdirectory(dcache ALL)
endif()
if(hdfs)
  add_root_subdirectory(hdfs ALL)
endif()
