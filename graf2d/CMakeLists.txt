if(root7)
  add_root_subdirectory(primitives ALL)
  add_root_subdirectory(gpadv7 ALL)
endif()
add_root_subdirectory(gpad ALL)
add_root_subdirectory(graf ALL)
add_root_subdirectory(postscript ALL)
add_root_subdirectory(mathtext ALL)
if(WIN32)
  add_root_subdirectory(win32gdk ALL)
elseif(x11)
  add_root_subdirectory(x11 ALL)         # special CMakeLists.txt
  add_root_subdirectory(x11ttf ALL)      # special CMakeLists.txt
endif()
if(asimage)
  add_root_subdirectory(asimage ALL)     # special CMakeLists.txt
endif()
if(qt)
  add_root_subdirectory(qt ALL)
endif()
if(gviz)
  add_root_subdirectory(gviz ALL)
endif()
if(fitsio)
  add_root_subdirectory(fitsio ALL)
endif()
if(cocoa)
  add_root_subdirectory(quartz ALL)
  add_root_subdirectory(cocoa ALL)
endif()
